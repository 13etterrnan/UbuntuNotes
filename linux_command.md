> 519  root：Project519	passwords：519519519<br/>
  405  root：lab515		passwords：chen123<br/>
      root：lab519		passwords：lab515


# 用户管理
## 新建和删除用户
命令|作用|说明
-|-|-
useradd -m -g组|新建用户名|添加新用户	
-m：|自动建立用户家目录|
-g：|指定用户所再的组，否则会新建一个和用户同名的组|
passwd 用户名|设置用户名密码|如果是普通用户，直接用passwd可以直接修改追加的账户密码
userdel -r 用户名|删除用户|-r选项会自动删除用户家目录
cat /etc/passwd \| grep 用户名|确认用户信息|新建用户后，用户信息会保存在/etc/passwd文件中
## 查看用户信息
命令|作用
-|-
id|查看用户UID和GID信息
who|查看当前所有登录的用户列表
whoami|查看当前登录用户的用户名
which|查询命令所在位置（which ls）
	
## 切换用户
命令|作用|说明
-|-|-
su - 用户名|切换用户，并切换目录|- 可以切换到用户家目录，否则保持位置不变
exit|推出当前登录账户

# 常用Linux命令
序号|命令|对应英文|作用
-|-|-|-
01|ls|list|查看当前文件夹下的内容
02|pwd|print work directory|查看当前所在文件夹
03|cd [目录名]|change directory|切换文件夹
04|touch [文件名]|touch|如果文件不存在，新建文件
05|mkdir [目录名]|make directory|创建目录
06|rm [文件名]|remove|删除指定文件名（rm -r xxx 可删除目录）
07|clear|clear|清屏

> 终端命令格式：command [-options] [parameter]
说明：
	command：命令名，相应功能的英文你单词或单词的缩写
	[-options]: 选项，可用来对命令进行控制，也可以省略
	[parameter]：传给命令的参数，可以是零个，一个或者多个
[ ]表示可选

# 查阅命令帮助信息
    --help：
        command --help
    man：
	    man command（man是manul缩写，是linux提供的一个手册，包含了绝大部分的命令，函数的详细使用说明）


# 命令扩展
* touch：<br/>
    * 如果文件不存在，可以新建一个空白文件<br/>
    * 如果文件已存在，可以修改文件的末次修改日期<br/>
* mkdir：
    * mkdir -p a/b/c/d/ （-p选项 可以递归创建目录，也就是创建a目录，然后a目录下有b目录...）
	> （ps：新建目录名不可与已存在的目录或文件同名）

* rm：
    > 删除文件或目录（ps：直接从磁盘中干掉，删除后不能恢复）
    * rm -r xxx	删除名为xxx的目录
	* rm -f xxx	删除名为xxx的文件（加上-f选项后，无论有没有名为xxx的文件将不提示信息）
	>（ps：rm -rf * 千万不要在根目录下使用，除非你跟老板有仇；千万不要在根目录下使用，除非你跟老板有仇；千万不要在根目录下使用，除非你跟老板有仇）
	
* tree：
    > tree命令可以以树状图列出文件目录结构
	
    * 例如：tree .	显示当前目录的文件目录结构<br/>
		    tree ~	显示当前用户目录的文件目录结构<br/>
		    tree -d .	只显示当前目录的目录结构（不显示文件）
* cp：
    * cp 源文件 目标文件    （复制文件或目录）	（ps：cp ~/xxx .	表示将用户目录下的xxx复制到当前目录，不改变xxx的名称
	* cp ~/xxx ./yyy		表示将用户目录下的xxx复制到当前目录，并重命名为yyy
	* -i：覆盖文件前提示
	* -r：若给出的源文件是目录文件，则cp将复制该目录下的所有子目录和文件，目标文件必须是个目录名
* mv：	
    * mv 源文件 目标文件    （移动文件或目录）
	* mv ./readme.txt ./demo.txt 	将源文件移动到目标文件并重命名


# 其他
* echo：<br/>
	会在终端中显示参数指定的文字，通常会和重定向联合使用
* 重定向：>和>><br/>
		>:表示输出，会覆盖文件原有的内容 <br/>
		>>:表示追加，会将内容追加到已有文件的末尾


# 文件内容有关的命令
* cat：<br/>
	cat 文件名		查看文件内容，创建文件，文件合并，追加文件内容等功能（文件内容较少时使用）
* more：<br/>
	more 文件名 	分屏显示文件内容（文件内容较多时使用）
* grep：<br/>	grep 搜索文本 文件名		搜索文本文件内容


# 远程管理命令
shutdown：
命令|功能
-|-
shutdown -r|	重新启动
shutdown -r now| 	重新启动系统，now表示现在
shutdown now|	立刻关机
shutdown 20.25| 	系统在今天的20.25会关机
shutdown -c| 	取消之前之前的关机计划
>tips：不指定选项和参数，默认1分钟之后 关闭电脑,远程维护服务器时，最好不要关闭系统，而应该重新启动系统
	
ifconfig:
命令|英文|功能
-|-|-
ifconfig|	configure a network interface| 查看/配置计算机当前的网卡配置信息
	
ping:
命令|英文|功能
-|-|-
ping ip地址|ping|检测到目标ip地址的连接是否正常


# 用户权限相关命令
权限|英文|缩写|数字代号
-|-|-|-
读|read|r|4
写|write|w|2
执行|execute|x|1

> ls -l 可以查看文件夹文件的详细信息，从左到右依次是：<br/>
		**权限**。第一个字符如果是d，则表示目录 <br/>
		**硬链接数**。通俗的将，就是有多少中方式，可以访问到当前目录/文件<br/>
		**拥有者**。家目录下 文件/目录的拥有者通常是当前用户<br/>
		**组**。在linux中，有很多时候，会出现组名和用户名相同的情况。后续再了解<br/>
		**大小**。<br/>
		**时间**。<br/>
		**名称**。


# 打包压缩
> windows常用rar；<br/>
	mac常用zip；<br/>
	Linux常用tar.gz
	
## tar与gzip命令结合可以实现文件 打包和压缩
* tar只负责打包，但不压缩
* gzip压缩tar打包后的文件，其扩展名一般用xxx.tar.gz

## tar是Linux中最常用的备份工具，可打包可恢复

* //打包文件<br/>
	tar -cvf 打包文件.tar 被打包的文件/路径。。。。（eg：tar -cvf py.tar 01.py 02.py 03.py）
* //解包文件<br/>
	tar -xvf 打包文件.tar	(eg:tar -xvf py.tar )

    > (ps:	<br/>
            c	生成档案文件，创建打包文件<br/>
			x	解开档案文件<br/>
			v	列出归档解档的详细过程，显示进度<br/>
			f	指定归档文件名称，f后面一定要是.tar文件，所有必须放在最后，其他的顺序可以随意
		)
	
## gzip
> tar命令中有一个选项-z可以调用gzip，从而可以方便的实现压缩和解压缩的功能
		
* 命令格式如下：
    * //压缩文件<br/>
		tar -zcvf 打包文件.tar.gz 被打包的文件/路径。。。。
	* //解压缩文件<br/>
		tar -zxvf 解包文件.tar.gz	(eg:tar -xvf py.tar )
	* //解压缩到指定路径<br/>
		tar -zcvf 打包文件.tar.gz -C 目标路径